<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:orientation="vertical"
    android:padding="14dp"
    android:background="#17171F">


    <!-- Cabeçalho -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Foto -->

        <ImageView
            android:id="@+id/ivCommentAuthorPhoto"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_default_profile"
            android:contentDescription="Foto do autor" />


        <!-- Nome -->

        <TextView
            android:id="@+id/tvCommentAuthorName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="10dp"
            android:text="Maria Souza"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold" />


        <!-- Nota -->

        <TextView
            android:id="@+id/tvCommentRating"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="★ 5.0"
            android:textColor="#FBBF24"
            android:textSize="13sp"
            android:textStyle="bold" />

    </LinearLayout>


    <!-- Comentário -->

    <TextView
        android:id="@+id/tvCommentText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="Ótimo jogador! Foi muito divertido jogar com ele."
        android:textColor="#D4D4D8"
        android:textSize="13sp"
        android:lineSpacingExtra="2dp" />


    <!-- Data -->

    <TextView
        android:id="@+id/tvCommentDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="7dp"
        android:text="Há 2 dias"
        android:textColor="#52525B"
        android:textSize="11sp" />

</LinearLayout>